# 随译 LinguaDesk 1.1

一个适用于 Windows 10 / 11 **64 位（x64）** 的桌面翻译工具。

## 安装与分享

把 `随译-安装程序-1.2.0.exe` 分享给别人即可。双击安装，默认安装到当前账户的 `%LOCALAPPDATA%\Programs\LinguaDesk`，无需管理员权限、Python、Node.js 或开发环境。新版带有独立图标，用于程序窗口、托盘、桌面/开始菜单快捷方式、右键入口及安装包。

默认免费翻译无需 API Key，需要联网。使用 Windows 自带 .NET Framework 与 OCR；精简系统可能缺少这些组件。此版本未进行商业代码签名。

## 随时翻译

| 操作 | 方法 |
| --- | --- |
| 选词翻译 | 在浏览器、编辑器等应用里选中文字，按 **Alt+Q** |
| 截图翻译 | 按 **Alt+W**，拖动框选文字；Esc 或右键取消 |
| 打开窗口 | 按 **Alt+E**，或双击系统托盘图标 |
| 划词浮钮 | 在主窗口勾选“划词浮钮”；在支持可访问性选区的应用里拖动选中文字，再点击“翻译” |
| 翻译文件 | 点击“打开文件”，或把一个文件拖入主窗口 |
| 翻译输入文字 | 粘贴到左侧，点击“翻译”或按 **Ctrl+Enter** |
| 保存译文 | 点击“另存译文”，导出 UTF-8 TXT |
| 完全退出 | 右键托盘图标 → 退出；关闭主窗口会驻留托盘 |

快捷键被其他程序占用时，状态栏会提示；可使用窗口按钮。
划词浮钮依赖目标应用提供可访问性选区，并非所有应用都支持；失败时使用 Alt+Q 或截图翻译。高权限应用、受保护页面、无法复制的内容可能无法选词读取。

## 自定义快捷键

上表列出的是默认快捷键。点击主窗口的 **快捷键设置**，或右键托盘图标 → **快捷键设置**，可分别修改选词翻译、截图翻译、打开窗口，以及窗口内翻译文字的快捷键。

勾选 Ctrl / Alt / Shift，再选择字母、数字、F1–F12 或 Enter；组合至少包含 Ctrl 或 Alt。点击 **保存并生效** 后立即生效，并在下次启动时保留。修改后以界面和托盘显示的快捷键为准。点击 **恢复默认** 后再保存，可恢复上表组合。

不同功能不能使用相同组合；被系统或其他软件占用的快捷键会显示冲突提示，原设置保持不变。F12 等系统保留组合在部分电脑上无法注册。Ctrl+C 留给复制与选词读取，Alt+F4 留给关闭窗口，不可用于绑定；也建议避开其他常用编辑快捷键。

## 右键入口

安装后，右键支持的文件 → **用随译翻译**。桌面或文件夹空白处右键 → **随译 LinguaDesk**，可选择截图翻译、翻译剪贴板、打开随译。Windows 11 通常需要先点击 **显示更多选项** 才能看到传统右键扩展。程序没启动时会自动启动，已经运行时会把请求交给当前窗口。处理中收到其他请求时会提示稍后重试。

托盘图标右键可翻译文件、截图和剪贴板；主窗口原文框右键可翻译选区/全文、复制粘贴，译文框右键可复制/另存。工具不修改浏览器或 Word 等其他软件内部的右键菜单；这些场景使用 Alt+Q 或划词浮钮。

这些菜单仅为当前 Windows 账户安装，卸载时一并移除。安装便携版文件夹本身不会注册系统右键入口，需要运行安装包。

## 文件范围

- TXT、MD、SRT、CSV、LOG：读取 UTF-8、UTF-16，兼容常见 GBK 文本。
- DOCX：提取正文与表格内文字；不处理旧版 DOC、页眉页脚、批注或图片内文字。
- PDF：逐页渲染后在本机 OCR，扫描版也可尝试；密码保护或损坏文件可能无法读取。
- PNG、JPG、JPEG、BMP、TIF、TIFF：本机 OCR；多帧图像仅处理默认帧。
- 每次处理一个文件，文件大小上限 80 MB。大 PDF 处理需要时间，可取消。

**这是文字翻译工具。文件译文统一导出为 TXT，不保留 Word/PDF 排版，也不保证字幕时间轴、CSV/Markdown 结构可直接作为原格式使用。** 原文件不会被修改。OCR 可能误识别字符，重要内容请校对。

## 语言与翻译服务

默认目标语言是简体中文。免费服务采用本地语言识别，支持中、英、日、韩、德、法、西班牙、俄、意大利、荷兰、葡萄牙语；AI 服务由模型识别语言。短词、混合语言、繁简体区分或 OCR 错误较多时，请手动选择原文语言。状态栏显示识别结果，并在中文译文仍保留大量拉丁字母原文时提示检查。选择与原文相同的目标语言时，会直接显示原文。

“服务设置”可选择：

1. **MyMemory 免费在线**：无需密钥；有额度与网络可达性限制，额度用尽时会显示错误，保留已经完成的译文。长文件建议配置自己的 AI 接口。
2. **自定义 AI 接口**：填写兼容 Chat Completions 的完整接口 URL、模型名、API Key。例如预填的 DeepSeek 地址需要你自己的服务账户与密钥；本工具不附赠额度。网络接口要求 HTTPS，本机地址允许 HTTP。

API Key 只保存在该使用者的 Windows 账户下，并通过 Windows DPAPI 加密；安装包不包含开发者或其他使用者的密钥。设置文件位于 `%LOCALAPPDATA%\LinguaDesk\settings.json`。

截图和 PDF 的识别在本机完成；提取出的文字会发送给选择的在线翻译服务。工具不保存翻译历史、不上传截图原文件；OCR 临时文件在完成或取消时清理。系统崩溃或强制终止可能留下临时文件。

OCR 需要 Windows 对应语言组件。缺少时，可在 Windows“设置 → 时间和语言 → 语言和区域”添加所需语言和相关文字识别功能。未安装所选 OCR 语言时，程序使用当前系统可用语言，效果可能受影响。

从 1.2.0 起，截图和图片中的小字会自动尝试两倍插值放大后再次识别，不需要额外设置。正常字号保留原尺寸识别；放大受系统识别器尺寸上限约束。放大可以改善小字识别，但无法恢复截图中未保留的笔画，特别小的字号、数字或复杂背景仍需校对。若仍有错字，可先提高原软件的显示缩放，再截图。

## 卸载与更新

先右键托盘图标选择“退出”。然后在 Windows“设置 → 应用 → 已安装的应用”中找到 **随译 LinguaDesk** 并卸载，也可使用开始菜单的“卸载随译”。卸载保留个人设置和你已保存的译文。需要移除个人设置时，可自行删除 `%LOCALAPPDATA%\LinguaDesk` 文件夹。

更新时，退出随译后重新运行新安装包即可。此版本没有自动更新、开机自启或账户同步。

## 服务参考

- MyMemory API：https://mymemory.translated.net/doc/spec.php
- MyMemory 额度说明：https://mymemory.translated.net/doc/usagelimits.php
- Windows OCR：https://learn.microsoft.com/en-us/uwp/api/windows.media.ocr.ocrengine

## 验证范围

已在当前 Windows 11 x64 环境验证本机图片/PDF OCR、DOCX 正文与表格提取、4 种文本编码、MyMemory 真实请求、模拟 AI 接口请求/响应、取消和错误处理。第三方 AI 账户未配置，未做实际付费模型请求。其他电脑的网络、OCR 语言包以及各应用的选区能力仍会影响使用。

## 1.2.0 · 2026-09-24

- 点击 ⇄ 同时互换语言和两侧文字。自动识别时使用本机识别的语言；空白原文请先选择语言。
- 主窗口底部增加开机自启动开关，登录后驻留托盘，卸载时移除启动项。如果曾在系统任务管理器禁用，需要在系统中恢复。
- 复用翻译连接，取消分段额外等待；同文字、语言和服务设置的最近 128 段译文仅在内存中缓存，退出即清除。首次在线翻译耗时仍受网络和服务影响。
## 1.3.0 更新

- 新增一键清空：同时清空原文、译文；正在处理时取消任务并清除迟到结果。
- 新增文字大小设置：9–28，两侧同步调整，自动记住设置。
- 新增窗口置顶开关，自动记住设置。
- 新增整理截图换行开关，默认开启，仅用于新截图。合并可能的行内折行，尽量保留空行、编号和句末换行；诗歌、表格或特殊排版建议关闭后重新截图。
- 清空保留语言、服务和快捷键设置，不删除已保存的译文文件。
