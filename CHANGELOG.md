## 1.3.0 更新

- 新增一键清空：同时清空原文、译文；正在处理时取消任务并清除迟到结果。
- 新增文字大小设置：9–28，两侧同步调整，自动记住设置。
- 新增窗口置顶开关，自动记住设置。
- 新增整理截图换行开关，默认开启，仅用于新截图。合并可能的行内折行，尽量保留空行、编号和句末换行；诗歌、表格或特殊排版建议关闭后重新截图。
- 清空保留语言、服务和快捷键设置，不删除已保存的译文文件。

## 1.2.0 · 2026-09-24

- 点击 ⇄ 同时互换语言和两侧文字。自动识别时使用本机识别的语言；空白原文请先选择语言。
- 主窗口底部增加开机自启动开关，登录后驻留托盘，卸载时移除启动项。如果曾在系统任务管理器禁用，需要在系统中恢复。
- 复用翻译连接，取消分段额外等待；同文字、语言和服务设置的最近 128 段译文仅在内存中缓存，退出即清除。首次在线翻译耗时仍受网络和服务影响。
# 更新记录

## 1.1.1

- 截图与图片识别增加小字自适应增强：识别字高不足 14 像素或首轮没有文字时，尝试两倍插值放大。
- 放大受 Windows 识别器最大图片尺寸约束，保持比例；放大识别失败或为空时保留原尺寸结果。
- 正常字号保持原尺寸识别，避免不必要的放大；清晰但极小的文字仍可能出现误识别。

## 1.1.0

- 新增独立应用图标，应用于窗口、托盘、快捷方式和安装程序。
- 新增四项自定义快捷键，支持立即生效、保存、恢复默认和冲突检测。
- 快捷键变更失败时保留原绑定。

## 1.0.0

- 选词、截图、文件与剪贴板翻译。
- Windows 当前用户安装器、卸载器和右键菜单入口。
- 本地语言识别与 MyMemory / 自定义 AI 翻译接口。
